<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Invitaciones</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            color: #333;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4;
        }
        .container {
            width: 100%;
            max-width: 600px;
            margin: 0 auto;
            background-color: #ffffff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        header {
            text-align: center;
            margin-bottom: 20px;
        }
        .encabezado {
            margin-bottom: 10px;
        }
        .titulo {
            font-size: 24px;
            color: #e91e63;
            margin-top: 2em;
        }
        h1 {
            font-size: 28px;
            margin: 0;
        }
        h2 {
            font-size: 20px;
            color: #555;
            margin: 10px 0;
        }
        .context-container {
            text-align: center;
        }
        .subtitulo {
            display: block;
            font-size: 18px;
            color: #666;
            margin-bottom: 10px;
        }
        a {
            display: inline-block;
            padding: 10px 20px;
            font-size: 16px;
            color: #ffffff;
            background-color: #e91e63;
            text-decoration: none;
            border-radius: 4px;
            transition: background-color 0.3s ease;
        }
        a:hover {
            background-color: #c2185b;
        }

        .link-url {
                   color: #ffffff;
                   text-decoration: none;
        }
    </style>
</head>
<body>
    <header>
        <div class="container">
            <img src="https://res.cloudinary.com/ddcrjdfrf/image/upload/v1722429939/celebria-icons/celebria-logo-pink.png" width="400" height="150" alt="celebria-logo-pink">
            <div class="encabezado">
                <h1 class="titulo">Hola !</h1>
            </div>
            <h2>{{name}} te ha invitado a su evento.</h2>
        </div>
    </header>
    <section class="context-container">
        <span class="subtitulo">No esperes más para aceptar la invitación !!</span>
        <span class="link-url"><a href="{{url}}">Acepta la invitación aquí</a></span>
    </section>
</body>
</html>
